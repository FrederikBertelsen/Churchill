{
    "name": "@logit/server",
    "version": "0.1.0",
    "main": "dist/index.js",
    "scripts": {
        "build": "mkdir -p dist && cp -r src/* dist/",
        "dev": "nodemon --watch src --ext js --exec 'npm run build'"
    },
    "dependencies": {
        "http": "0.0.1-security"
    },
    "devDependencies": {
        "nodemon": "^2.0.20"
    }
}