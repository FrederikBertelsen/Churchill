{
    "name": "churchill-logger",
    "version": "1.0.0",
    "main": "./src/index.js",
    "scripts": {
        "build": "mkdir -p dist && cp -r src/* dist/"
    },
    "dependencies": {
        "http": "0.0.1-security"
    }
}