const defaultLevels = {
    fatal: 10,
    error: 20,
    warn: 30,
    info: 40,
    debug: 50,
    trace: 60,
};


const defaultOptions = {
    levels: defaultLevels,
    level: 'info',
};

module.exports = defaultOptions;