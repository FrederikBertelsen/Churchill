{
  "name": "logit-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r dev"
  },
  "devDependencies": {
    "esbuild": "^0.18.0",
    "jest": "^29.0.0",
    "eslint": "^8.0.0"
  }
}