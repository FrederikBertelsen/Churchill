{
  "name": "churchill-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r dev"
  },
  "devDependencies": {
    "esbuild": "^0.18.0",
    "eslint": "^9.0.0"
  }
}